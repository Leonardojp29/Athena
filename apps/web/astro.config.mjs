// @ts-check
import { defineConfig } from 'astro/config';

// Fase 0: static placeholder. SSR adapter + React islands + Tailwind land in Fase 2.
export default defineConfig({
  site: 'https://athena.example.com',
});
