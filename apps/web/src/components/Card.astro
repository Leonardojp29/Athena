---
interface Props {
  title?: string;
}
const { title } = Astro.props;
---

<section class="bg-surface border border-border rounded-lg p-4">
  {title && <h2 class="text-base font-semibold mb-3">{title}</h2>}
  <slot />
</section>
